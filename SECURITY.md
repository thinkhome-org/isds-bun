# Security

Do not report vulnerabilities in public issues when they include credentials,
message contents, private keys, certificates, ZFO data, or personal data.

The SDK must not log passwords, authorization headers, cookies, private keys,
message bodies, attachments, SOAP bodies, or full personal data by default.
